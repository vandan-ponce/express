apiVersion: apps/v1
kind: Deployment
metadata:
  name: express-app
  namespace: express-app
spec:
  replicas: 4
  selector:
    matchLabels:
      app: express-app
  template:
    metadata:
      labels:
        app: express-app
        name: vandan
    spec:
      containers:
      - name: express-app
        image: public.ecr.aws/z3s7x0s4/vandan-express-app:v2
        imagePullPolicy: Always
        ports:
        - containerPort: 3000

---

apiVersion: v1
kind: Service
metadata:
  name: express-app
  namespace: express-app
  labels: 
    lead: sy
spec:
  selector:
    app: express-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer